{
  "name": "squidgame100-minecraft",
  "version": "0.1.0",
  "private": true,
  "description": "MineCraft — a browser voxel survival sandbox with real Earth terrain, wiki textures, and streaming infinite chunks.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/shared",
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm -w @sg100/shared run build && npm run typecheck --workspaces --if-present",
    "dev:server": "npm -w @sg100/server run dev",
    "generate:map": "node tools/generate-project-map.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
