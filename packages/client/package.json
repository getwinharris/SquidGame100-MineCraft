{
  "name": "@sg100/client",
  "version": "0.1.0",
  "private": true,
  "description": "SquidGame100 MineCraft browser client — Canvas 2D.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sg100/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
